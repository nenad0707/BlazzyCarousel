@page "/docs/installation"

<PageTitle>Installation - BlazzyMotion</PageTitle>

<PageHeader Title="Installation"
            Icon="📦"
            Subtitle="Get started with BlazzyMotion in your Blazor project"
            Version="1.1.0"
            Framework=".NET 8+" />

<DocSection Title="Prerequisites" Icon="✅">
    <p>Before installing BlazzyMotion, make sure you have:</p>
    <ul class="bz-list">
        <li>.NET 8.0 SDK or later</li>
        <li>A Blazor WebAssembly or Blazor Server project</li>
        <li>Visual Studio 2022,Visual Studio 2026, VS Code, or JetBrains Rider</li>
    </ul>
</DocSection>

<DocSection Title="Install via NuGet" Icon="📥">
    <p>The easiest way to install BlazzyMotion is through NuGet Package Manager.</p>

    <h4 class="bz-mt-3">.NET CLI</h4>
    <CodeBlock Language="bash" Code="@_cliInstall" />

    <h4 class="bz-mt-3">Package Manager Console</h4>
    <CodeBlock Language="powershell" Code="@_pmcInstall" />

    <h4 class="bz-mt-3">PackageReference</h4>
    <CodeBlock Language="xml" Code="@_packageRef" />
</DocSection>

<DocSection Title="Configuration" Icon="⚙️">
    <p>BlazzyMotion uses Source Generators, so there's <strong>no service registration required!</strong></p>
    <p><strong>CSS is automatically included</strong> - no manual setup needed! ✨</p>

    <p class="bz-mt-2">Just add the using statement to your <code>_Imports.razor</code>:</p>
    <CodeBlock Language="razor" Code="@_imports" />
</DocSection>

<DocSection Title="Verify Installation" Icon="🔍">
    <p>Create a simple test to verify everything is working:</p>
    <CodeBlock Language="razor" Code="@_testCode" />
    <p class="bz-mt-2">If you see the carousel rendered, you're all set! 🎉</p>
</DocSection>

<DocSection Title="Next Steps" Icon="🚀">
    <div class="bz-flex bz-gap-2" style="flex-wrap: wrap;">
        <a href="/docs/quick-start" class="bz-btn bz-btn--primary">
            Quick Start Guide →
        </a>
        <a href="/" class="bz-btn bz-btn--secondary">
            View Live Demo
        </a>
    </div>
</DocSection>

@code {
    private string _cliInstall = "dotnet add package BlazzyMotion.Carousel";

    private string _pmcInstall = "Install-Package BlazzyMotion.Carousel";

    private string _packageRef = "<PackageReference Include=\"BlazzyMotion.Carousel\" Version=\"1.1.0\" />";

    private string _imports = @"@using BlazzyMotion.Core.Models
@using BlazzyMotion.Carousel.Components";

    private string _testCode = @"@page ""/test""

<BzCarousel Items=""@_movies"" Theme=""BzTheme.Glass"" />

@code {
    private List<Movie> _movies = new()
    {
        new Movie { ImageUrl = ""/images/movie1.jpg"", Title = ""Test"" }
    };

    // Define your model with [BzImage] attribute
    public class Movie
    {
        [BzImage]
        public string ImageUrl { get; set; } = """";

        public string Title { get; set; } = """";
    }
}";
}